function hash(str) {
    return ''
}

function compare(plainStr, hashedStr) {
    return true
}

function generateToken(payload) {
    return {}
}

module.exports = {
    hash,
    compare,
    generateToken
}