const connect = () => {
    return Promise.resolve()
}

const disconnect = () => {
    return Promise.resolve()
}

const clearDB = async () => {
    return Promise.resolve()
}

module.exports = {
    connect, disconnect, clearDB
}